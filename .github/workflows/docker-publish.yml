<<<<<<< HEAD
name: Docker

# 🚀 Workflow para construir y publicar imágenes Docker en GitHub Container Registry
# Funciona tanto en la rama main como en la rama dos_capas
=======
name: Docker Publish
>>>>>>> 2_capas

on:
  schedule:
    - cron: '35 13 * * *'
  push:
<<<<<<< HEAD
    branches: [ "main", "dos_capas" ]
    tags: [ 'v*.*.*' ]
  pull_request:
    branches: [ "main" ]
=======
    branches: [ "dos_capas" ]
    tags: [ 'v*.*.*' ]
  pull_request:
    branches: [ "dos_capas" ]
>>>>>>> 2_capas

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build:
<<<<<<< HEAD

    runs-on: ubuntu-latest
=======
    runs-on: ubuntu-latest

>>>>>>> 2_capas
    permissions:
      contents: read
      packages: write
      id-token: write

    steps:
<<<<<<< HEAD
      - name: 📥 Checkout del repositorio
        uses: actions/checkout@v4

      # Instalación de cosign para firma de imágenes
      - name: 🔐 Instalar cosign
        if: github.event_name != 'pull_request'
        uses: sigstore/cosign-installer@59acb6260d9c0ba8f4a2f9d9b48431a222b68e20 # v3.5.0
        with:
          cosign-release: 'v2.2.4'

      # Configurar Buildx para compilación multi-plataforma
      - name: 🧱 Configurar Docker Buildx
        uses: docker/setup-buildx-action@f95db51fddba0c2d1ec667646a06c2ce06100226 # v3.0.0

      # Login en el registry
      - name: 🔑 Iniciar sesión en el registro ${{ env.REGISTRY }}
        if: github.event_name != 'pull_request'
        uses: docker/login-action@343f7c4344506bcbf9b4de18042ae17996df046d # v3.0.0
=======
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install cosign
        if: github.event_name != 'pull_request'
        uses: sigstore/cosign-installer@v3.5.0
        with:
          cosign-release: 'v2.2.4'

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log into registry ${{ env.REGISTRY }}
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
>>>>>>> 2_capas
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

<<<<<<< HEAD
      # Metadatos para imagen Node
      - name: 🏗️ Extraer metadatos Docker para APP
        id: meta-app
        uses: docker/metadata-action@96383f45573cb7f253c731d3b3ab81c87ef81934 # v5.0.0
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-node

      # Construcción y subida de imagen Node
      - name: 🚀 Build y Push de imagen Docker para APP
        id: build-and-push-node
        uses: docker/build-push-action@0565240e2d4ab88bba5387d719585280857ece09 # v5.0.0
        with:
          context: ./node
=======
      # ------------------------------
      # APP NODE
      # ------------------------------
      - name: Extract Docker metadata for APP_NODE
        id: meta-app
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-app_node

      - name: Build and push Docker image for APP_NODE
        id: build-and-push-app
        uses: docker/build-push-action@v5
        with:
          context: ./app_node
          file: ./app_node/dockerfile
>>>>>>> 2_capas
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta-app.outputs.tags }}
          labels: ${{ steps.meta-app.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

<<<<<<< HEAD
      # Metadatos para imagen PostgreSQL
      - name: 🏗️ Extraer metadatos Docker para POSTGRES
        id: meta-postgres
        uses: docker/metadata-action@96383f45573cb7f253c731d3b3ab81c87ef81934 # v5.0.0
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-postgres

      # Construcción y subida de imagen PostgreSQL
      - name: 🐘 Build y Push de imagen Docker para POSTGRES
        id: build-and-push-postgres
        uses: docker/build-push-action@0565240e2d4ab88bba5387d719585280857ece09 # v5.0.0
        with:
          context: ./postgres
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta-postgres.outputs.tags }}
          labels: ${{ steps.meta-postgres.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # Firma de la imagen publicada
      - name: 🧾 Firmar la imagen Docker publicada
        if: ${{ github.event_name != 'pull_request' }}
        env:
          TAGS: ${{ steps.meta-app.outputs.tags }}
          DIGEST: ${{ steps.build-and-push-node.outputs.digest }}
        run: echo "${TAGS}" | xargs -I {} cosign sign --yes {}@${DIGEST}
=======
      # ------------------------------
      # POSTGRES / BBDD
      # ------------------------------
      - name: Extract Docker metadata for APP_BBDD
        id: meta-bbdd
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-app_bbdd

      - name: Build and push Docker image for APP_BBDD
        id: build-and-push-bbdd
        uses: docker/build-push-action@v5
        with:
          context: ./app_bbdd
          file: ./app_bbdd/dockerfile
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta-bbdd.outputs.tags }}
          labels: ${{ steps.meta-bbdd.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # ------------------------------
      # SIGN IMAGES
      # ------------------------------
      - name: Sign the published Docker images
        if: ${{ github.event_name != 'pull_request' }}
        env:
          TAGS_APP: ${{ steps.meta-app.outputs.tags }}
          DIGEST_APP: ${{ steps.build-and-push-app.outputs.digest }}
          TAGS_BBDD: ${{ steps.meta-bbdd.outputs.tags }}
          DIGEST_BBDD: ${{ steps.build-and-push-bbdd.outputs.digest }}
        run: |
          echo "${TAGS_APP}" | xargs -I {} cosign sign --yes {}@${DIGEST_APP}
          echo "${TAGS_BBDD}" | xargs -I {} cosign sign --yes {}@${DIGEST_BBDD}
>>>>>>> 2_capas
